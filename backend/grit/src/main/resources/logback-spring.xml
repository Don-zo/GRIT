<configuration>
  <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">

    <labels>
      service = grit
      application = grit
      app = grit
      level = %level
    </labels>

    <message>
      <pattern>%d{yyyy-MM-dd} %d{HH:mm:ss.SSS} trace_id=%X{traceId:-NONE} span_id=%X{spanId:-NONE}
        trace_flags=%X{traceFlags:-NONE} %-5level [%thread] --- %logger{36} - %msg%n
      </pattern>
    </message>

    <http>
      <url>http://192.168.32.152:3101/loki/api/v1/push</url>
    </http>

    <batch>
      <maxItems>500</maxItems>
      <timeoutMs>5000</timeoutMs>
    </batch>

  </appender>

  <root level="INFO">
    <appender-ref ref="LOKI"/>
  </root>

</configuration>
